# 数据采集方法

## 1. 采集目的与研究范围

项目围绕 X 平台涉疆议题构建 2021—2025 年纵向数据，用于识别低可见度但持续参与议题、具有相对网络优势的知情代理人式主体，并分析其身份结构、行为节律、关注关系、评论互动和事件阶段角色。

观察窗口为 2021 年 1 月 1 日至 2025 年 12 月 31 日。研究只处理公开可见账号、帖文和互动信息，不访问私信、受保护内容或非公开数据。

## 2. 数据来源与采集分工

数据由两部分组成：

1. **历史整理数据（2021—2023）：** 对已有 X 平台导出与人工整理表进行账号匹配、日期解析、字段统一和重复核验；
2. **爬虫补采与复核数据（2024—2025）：** 使用项目根目录的 `x_scraper.py`，通过 Selenium + Chrome 读取公开页面，采集账号时间线、X 高级搜索结果、单帖详情和可见评论。

当前爬虫是数据补采和复核工具，不把 2021—2023 年历史表描述成由当前版本一次性重爬所得。五个年度在进入分析前统一字段、时间口径和账号 ID。

## 3. 候选样本形成

初始候选池包括协助国际分众识别形成的账号、涉疆关键词搜索账号、公开组织/媒体/人物互动网络中的个体账号和后续网络扩展账号，共 1,128 个。经账号属性、有效涉疆记录和可持续观察性核验后形成 557 个总体有效账号。

在 557 个总体账号上依次应用四维标准：

| 维度 | 操作化标准 |
|---|---|
| 账号属性 | 个人注册账号；排除政府、媒体、NGO、企业等机构账号 |
| 议题参与 | 观察期内涉疆帖文不少于 20 条，且涉疆内容占总发帖不低于 30% |
| 影响力量级 | 粉丝数位于 1,000—100,000 区间 |
| 网络位置 | 度中心性或中介中心性高于总体均值 1 个标准差 |

四维标准联合筛得 206 个知情代理人子样本，其中社会活动型 117 个、学者型 43 个、普通用户型 46 个。

## 4. 运行环境

- Python 3.11 及以上
- Google Chrome
- Selenium
- 可选：undetected-chromedriver

安装：

```bash
pip install -r requirements.txt
```

`requirements.txt` 只列 `x_scraper.py` 实际导入的第三方库。CSV、JSON、日期、正则和命令行处理均使用 Python 标准库。

## 5. 爬虫模式

| 模式 | 用途 | 示例 |
|---|---|---|
| `tweet` | 采集单条帖文 | `python3 x_scraper.py tweet <帖文ID>` |
| `timeline` | 采集指定账号时间线 | `python3 x_scraper.py timeline <账号> --count 50` |
| `search` | 采集关键词搜索结果 | `python3 x_scraper.py search "Xinjiang" --count 50 --product Latest` |
| `account-search` | 按账号、任一关键词和日期进行高级搜索 | `python3 x_scraper.py account-search <账号> --since 2024-01-01 --until 2025-12-31` |
| `replies` | 核对单帖对话链及可见回复 | `python3 x_scraper.py replies <帖文ID> --count 30` |
| `report` | 汇总账号帖文与评论文件 | `python3 x_scraper.py report <账号> --since 2024-01-01 --until 2025-12-31` |

## 6. 实际采集流程

1. 明确账号、关键词、起止日期、目标数量和评论深度；
2. 记录运行日期、Git 提交、命令和不含凭据的配置摘要；
3. 启动前校验账号名、帖文 ID、日期顺序、数量和输出路径；
4. 使用统一导航重试、首屏异步等待、小步滚动和限流；
5. 每次 DOM 更新后重新查询元素，展开可见的“显示更多”正文；
6. 先提取稳定 ID、作者与时间，再执行日期、作者和相关性过滤；
7. 保存正文、互动数、原始链接、媒体数量和媒体证据链接；
8. 需要评论时进入详情页，在“Discover more / 更多推文”边界前保留对话链；
9. 输出 UTF-8 BOM CSV，并记录实际数量、跳过数量和停止原因；
10. 采集后执行唯一性、字段完整性、时间范围、作者归属和人工抽查。

## 7. 时间与相关性口径

X 页面时间通常为 UTC，程序统一转换为北京时间（UTC+8）后比较 `since` 和 `until`。日期停止判断先于关键词过滤，以免旧的不相关帖文阻断早停判断。

相关性审核分为：

- `新疆 / Xinjiang / XUAR` 等直接地域词；
- `维吾尔 / Uyghur / Uighur / Uygur` 和 `东突 / East Turkistan / East Turkestan` 等身份或政治词；
- 中国语境、事件语境、外链与媒体证据。

仅出现身份词而缺少中国/新疆语境的候选，不自动纳入；需要正文、外链或图片提供可复核证据。

## 8. 输出字段

帖文主要字段包括账号 ID、作者、时间、正文、转发、评论、点赞、浏览、帖文 ID、原始链接、媒体数量和媒体链接。评论主要字段包括评论者、评论 ID、原始链接、时间、正文、目标账号和对话链关系。

外部文本若以 `= + - @` 等公式前缀开头，导出前进行转义，防止表格软件把文本当作公式执行。

## 9. 关系数据与分析准备

关注关系按年度分别整理，保留关系方向；评论关系按评论者指向被评论账号，重复评论合并为边权。关注、评论、回复、转发和提及不混为同一种边。网络图和年度结构指标由 Gephi 与统计工具完成，终版图保存在 `绘图outputs/终版图片/`。

事件比较统一使用形成期（第 1—3 天）、发展期（第 4—10 天）和消弭期（第 11—30 天），并保留缺失阶段而不以 0 替代。

## 10. 限流、安全与伦理

- 使用随机等待、批次长暂停和限流冷却，不以并发刷新追求数量；
- Cookie 与密码等价，不进入 Git、文档、截图或最终提交包；
- `config.json` 和 `x_cookies*.json` 仅用于本机运行；
- 只采集研究所需的公开信息，报告优先呈现汇总结果；
- 不根据公开平台行为推断个人私下动机、违法性或未经证实的组织关系。

## 11. 可复核性

正式分析记录数据来源、时间范围、账号匹配、清洗规则、Git 版本、关键统计和图表批次。论文最终数字统一收录在 `data/整理版最终数据/论文最终统计口径.csv`，旧版工作簿和历次绘图继续保留，作为智能体协作、错误发现和迭代修订的过程证据。
