# CLAUDE.md

This file provides guidance to Claude Code when working with this repository.

## 项目目标

本项目使用 Selenium + Chrome 采集 X（Twitter）公开帖文、用户时间线、搜索结果、评论及子评论，并导出 UTF-8 BOM CSV，供2021—2025年涉疆议题知情代理人研究使用。

本项目的优先级始终是：

1. **数据归属正确**，不把推荐帖、转帖或无关评论归给目标账号；
2. **结果可复核**，每条记录保留稳定ID、作者、时间和来源链接；
3. **研究可重复**，采集口径、配置、代码版本和输出模式可追踪；
4. **凭据安全**，Cookie和本地配置不得进入Git或提交包；
5. 在以上条件满足后，再优化抓取数量和速度。

宁可少抓并明确记录跳过原因，也不能用低置信度数据填满结果。

## 项目文件

| 文件 | 说明 |
|---|---|
| `x_scraper.py` | X平台爬虫主程序 |
| `test_x_scraper.py` | 不依赖真实浏览器的回归测试 |
| `requirements.txt` | 采集、清洗、分析、绘图和证据整理所需第三方依赖 |
| `README.md` | 工具安装、运行方法、数据结果和项目导航 |
| `.gitignore` | Cookie、配置、缓存和输出忽略规则 |
| `数据采集方法.md` | 历史数据整理、爬虫补采和四维筛选方法 |
| `清洗报告.md` | 过程母表、最终统计口径和质量核查 |
| `fix_log.md` | Git提交、数据问题、修复和测试的对应关系 |
| `data/整理版最终数据/论文最终统计口径.csv` | 论文、图表和过程文档共同使用的最终数字 |
| `2244313043-李荣-大作业报告.md/.pdf` | 最终论文成品，只读，不在工具维护中改写 |

## 修改前必做

1. 先阅读与任务相关的现有函数和测试，不凭函数名猜测行为。
2. 明确本次修改影响的模式：`tweet`、`timeline`、`search`、`replies` 或 `report`。
3. 明确输入、输出CSV字段、时间口径、作者归属和停止条件是否变化。
4. 修复数据错误前，先构造一个最小失败样例或测试，确认能稳定复现。
5. 不修改与当前任务无关的文件，不顺手重构整个爬虫。

## 爬虫Harness

### 1. 不同语义禁止复用同名变量

连续提取正文、链接文字、作者名、作者账号时，必须使用不同变量，例如 `tweet_text`、`link_text`、`author_name`、`author_handle`。禁止把多个含义先后存入 `text`、`name` 等通用变量，否则会出现字段被静默覆盖。

### 2. 动态选择器和URL片段必须先验证

任何来自页面或用户输入的值，在用于CSS选择器、XPath或URL之前必须：

- 判空；
- 校验格式；
- 必要时转义或编码；
- 对账号名使用允许字符规则；
- 对推文ID只接受纯数字。

禁止让空账号生成 `a[href="/"]`，也禁止把带查询参数的字符串直接当推文ID。

### 3. 选择器必须用真实DOM验证

新增或修改选择器前，先在一个真实页面检查元素的 `outerHTML`，确认属性所在层级。不要凭经验假设互动数位于 `<article>`；X页面的互动信息可能位于内部 `div[role="group"]`。

验证时至少覆盖：

- 原创帖；
- 转帖或引用帖；
- 回复；
- 推荐帖；
- 中英文界面中的一个样例。

### 4. DOM变化后必须重新查询元素

每次滚动、点击、导航、刷新或展开评论后，旧的 Selenium 元素引用都视为失效。必须重新执行 `find_elements()`，不得跨DOM变化继续使用旧列表或旧元素。

### 5. JavaScript提取结果与Python字段必须同步

页面批量提取脚本返回的字段，是Python过滤、评论归属和CSV导出的接口契约。修改JavaScript字段时，必须同步检查：

- `_scroll_to_load()`；
- `_is_direct_reply()`；
- 评论递归逻辑；
- `export_posts_csv()`；
- `export_comments_csv()`；
- 对应单元测试。

禁止只改提取端而让下游默默得到空字段。

### 6. 时间线必须严格校验作者

用户时间线模式只保留 `author_handle` 与目标账号精确匹配的记录，比较时统一去掉前导 `@` 并使用 `casefold()`。

页面中出现以下内容不能自动归为目标账号帖文：

- 推荐帖；
- 其他账号的普通帖文；
- 页面插入的趋势或推广内容；
- 作者字段缺失的记录。

是否保留目标账号的转帖或引用帖必须由明确配置决定，不能因页面位置接近而推断。

### 7. 评论按对话链边界归属

帖子详情页中收集目标帖之前的对话链前文、目标帖之后的直接回复，
以及评论的二级、三级回复。`reply_to_handles` 用于区分回复层级，
但不再作为收集的必要条件。

程序必须在“Discover more / More Tweets / 更多推文”等推荐区标题处停止，
并排除明确的推广内容。递归抓取时，每条记录保留 `parent_tweet_id`、
`parent_author`、`depth` 和稳定链接；默认深度为 2，覆盖到三级回复。

### 8. 日期一律按北京时间比较

X页面时间通常为UTC。所有 `since`、`until`、CSV日期和提前停止判断统一转换为北京时间（UTC+8）后比较。

必须为UTC跨日边界保留测试，例如UTC日期与北京时间日期不同的情况。不得在字符串截断后直接比较日期。

### 9. 日期停止判断必须早于关键词过滤

提前停止滚动依赖页面中所有可解析帖文的最早日期，而不是只依赖关键词命中的帖文。即使旧帖不匹配关键词，也必须参与日期边界判断，否则会无意义地继续滚动。

处理顺序应为：

1. 提取并去重当前批次记录；
2. 解析时间并更新页面日期状态；
3. 判断是否达到日期停止条件；
4. 再执行作者、关键词等业务过滤；
5. 将合格记录加入结果。

### 10. 停止条件不能只看匹配数量

“连续无新增”应依据页面是否出现新的稳定帖文ID，而不是依据本轮是否新增了关键词匹配结果。连续出现不相关但更新的帖文不等于页面停止加载。

每次新的抓取任务开始前，必须重置 `_stop_early`、已见ID、连续停滞次数等运行状态，避免上一次任务污染下一次任务。

### 11. 使用小步滚动和显式等待

优先按视口高度小步滚动，不直接反复跳到 `document.body.scrollHeight`。滚动后使用可解释的等待条件或有限暂停，并重新读取页面数据。

滚动上限、无新增上限、目标数量和日期早停必须同时存在，避免无限循环。

### 12. 所有导航统一走重试封装

`driver.get()`、登录跳转、推文页面和评论页面导航统一通过带重试的函数完成。基本要求：

- 默认至少3次；
- 超时和限流分别处理；
- 重试间隔递增；
- 最终失败返回清晰状态，不假装抓取成功；
- 单页失败尽量不破坏已完成的数据。

禁止在业务函数中新增裸 `driver.get()` 而绕过重试、页面就绪检查和限流处理。

### 13. 限流必须集中管理

每次页面操作前后使用统一的 `RateLimiter`，保留：

- 固定区间内的随机等待；
- 每若干请求后的长暂停；
- 检测到限流后的强制冷却；
- 配置值的合理上下界。

不得通过高频刷新、并发浏览器或绕过访问限制追求数量。只采集公开可见且研究所需的数据。

### 14. 关键词匹配要区分实体词和泛化语境词

英文实体词使用单词边界或等价规则，避免子字符串误匹配。泛化词不能单独触发涉疆判定，必须与明确实体或语境共同出现。

修改关键词表时必须同时添加：

- 至少一个应命中的正例；
- 至少一个相似但不应命中的反例；
- 大小写或词形变化测试。

### 15. 互动数解析必须兼容语言和缩写

互动数据可能以英文、中文以及 `K/M/B/万/亿` 等形式出现。新增解析规则时要验证：

- 千位分隔符；
- 小数缩写；
- 缺失值；
- 中英文标签；
- 无法解析时的默认值和日志。

不得把无法解析的值悄悄当成真实的0而不留下质量提示。

### 16. CSV输出必须安全且结构稳定

输出要求：

- CSV使用 `utf-8-sig`，确保Excel打开不乱码；
- 外部文本若以 `= + - @` 等公式前缀开头，必须经过 `sanitize_csv_value()`；
- 固定字段顺序，不随字典顺序变化；
- 账号名、推文ID和链接按文本处理；
- 同一模式的CSV表头变更必须同步README、分析脚本和测试；
- 输出文件名包含可追踪时间或批次，不覆盖已有原始数据。

### 17. 稳定ID去重，禁止用正文去重

优先使用推文ID作为唯一键。正文相同可能是合法重复发布、转述或空正文，不能作为唯一去重依据。

去重时记录原始行数、重复行数和最终行数。若ID缺失，该记录应进入异常统计或单独处理，不能与正常记录混在一起。

### 18. 输入和配置必须在启动阶段校验

启动浏览器前验证：

- 推文ID格式；
- 账号名格式；
- 日期格式和起止顺序；
- 数量、递归深度、暂停时间和重试次数的范围；
- 输出目录是否可创建；
- Cookie文件是否存在。

错误输入应尽早失败并给出可操作提示，不要在运行数分钟后才报错。

### 19. Cookie按登录凭据处理

Cookie与密码等价。必须遵循：

- `x_cookies*.json`、`config.json` 永不进入Git；
- 日志、异常和截图中不打印Cookie值；
- POSIX系统尽量将Cookie权限收紧为 `600`；
- README只提供 `config.example.json` 或生成命令；
- 最终ZIP不得包含真实Cookie，即使 `.gitignore` 已忽略；
- 代码不得硬编码账号、密码、令牌或本机绝对凭据路径。

### 20. 每个数据Bug必须有回归测试

修复以下类型问题时，必须先补或同步补测试：

- 作者归属；
- 评论父子关系；
- 日期与时区边界；
- 关键词误匹配；
- 提前停止；
- 输入校验；
- CSV公式注入；
- 数字解析与输出字段。

测试使用最小字典、假Driver或纯函数，不依赖真实登录、实时页面或个人Cookie。

### 21. 正文完整性必须按DOM结构保证

帖文正文不得只读取第一个子节点、第一段或单一文本片段，也不能默认 `innerText` 在X所有DOM形态下都等于完整正文。提取时必须按页面顺序遍历正文根节点，并明确处理：

- 所有文本节点，包括嵌套 `span`、链接前后文字和多语种文本；
- `<br>` 以及段落型块元素产生的换行；
- 以 `img` 渲染的表情符号，优先保留其 `alt`，其次使用 `aria-label`；
- 表情、链接或其他嵌套节点之后的后续文字，遍历不得在中间节点提前终止；
- 隐藏的辅助节点或重复的无障碍文本，避免同一内容被重复拼接。

只允许规范化换行、不间断空格和多余空白；禁止按字符数截断正文，也禁止把页面显示的省略号当成已经获取全文的证据。

### 22. 展开折叠正文必须精准且可追踪

每次批量提取和单帖提取前，先处理X专用的帖文正文 “Show more / 显示更多” 控件。必须遵守：

- 只点击经验证的正文展开控件，不使用泛化的“更多”文本或按钮选择器；
- 每次点击后都重新查询DOM，不复用点击前的 Selenium 元素引用；
- 设置单轮最大展开次数，避免页面异常导致无限点击；
- 展开失败不得伪装成全文成功，应记录警告或完整性状态。

### 23. 主帖正文与引用帖必须隔离

同一个 `article[data-testid="tweet"]` 内可能同时出现主帖、引用卡片、回复上下文和多个 `/status/` 链接。正文、作者、帖文ID和原始链接必须来自同一条主帖语义边界。

禁止将引用帖正文拼到主帖正文中，也禁止用页面中遇到的第一个任意状态链接盲目代表主帖。如果无法唯一确定边界，应跳过并记录异常，不得混合输出。

### 24. 原始链接是可复核性必备字段

帖子和评论的CSV都必须保留可直接访问的原始状态链接。链接必须：

- 由已验证的作者账号和纯数字帖文ID构成；
- 规范化为 `https://x.com/{handle}/status/{tweet_id}` 形式，去掉跟踪查询参数；
- 链接中的帖文ID必须与同行 `tweet_id` 一致；
- 作为固定CSV字段保持顺序稳定，不能只在屏幕日志中显示。

若原始链接缺失或与ID不一致，该行必须进入质量异常统计，不得默认为可复核记录。

### 25. 评论过量时优先保证原帖质量

评论链可能迅速扩张、混入无关热门对话或触发限流。当评论数量过大、边界无法可靠确定或页面资源不足时，允许降低评论深度或关闭评论抓取，但必须：

- 先完整保存经作者、日期和相关性校验的原帖；
- 记录本次使用的评论深度、是否关闭评论以及中断原因；
- 将未完整的评论文件明确标记为“不完整/仅供核验”，不得声称已抓取全部评论；
- 评论抓取失败不得删除或覆盖已成功保存的原帖结果。

批量账号任务按账号顺序串行完成；开始下一账号前重置运行状态，避免上一账号的帖文ID、早停标记或评论状态污染后续任务。


## 采集后数据质量验收

一次抓取“程序没有报错”不等于数据合格。正式用于论文前，必须完成以下检查：

1. **作者准确率：** 时间线文件中非目标作者记录数及原因；
2. **唯一性：** 总行数、唯一帖文ID数、重复ID数；
3. **完整性：** 正文、时间、作者、链接、父帖ID的缺失率；
4. **时间范围：** 最早/最晚时间是否符合参数，UTC转北京时间是否正确；
5. **字段合理性：** 互动数是否为非负数，异常极大值是否可回查；
6. **评论归属：** 随机人工抽查内容是否位于目标帖对话链内，且未进入推荐区；
7. **数量解释：** 实际数量少于请求数量时记录是页面不足、日期停止、过滤还是加载失败；
8. **人工抽查：** 每个输出至少抽查头部、中部和尾部记录，不只看前几行。

若作者污染、重复、空正文或时间越界超出可接受范围，不得直接进入清洗和论文分析，应先修复采集逻辑或记录明确的数据排除方案。

## 研究可复现性

每次正式采集建议保留一份不含凭据的运行记录，至少包括：

- Git提交哈希；
- 运行命令和模式；
- 目标账号或检索词；
- `since`、`until`、数量和评论深度；
- 采集开始与结束时间；
- 使用的时区；
- 实际输出行数和跳过数；
- 配置参数摘要，但不含Cookie内容和本机敏感路径。

原始CSV只追加新批次，不直接覆盖。清洗后的数据必须写入新文件，并通过清洗日志与原始文件对应。

## 论文与数据口径对齐

最终论文研究范围为 X 平台涉疆议题 2021—2025 年纵向研究。所有过程文档更新前，先核对最终论文的题目、研究问题、样本量、年度序列和图表，不得继续沿用早期“2024—2025 年探索性研究”表述。

最终统计统一以 `data/整理版最终数据/论文最终统计口径.csv` 为入口。过程母表可以保留旧版明细和清洗痕迹，但必须明确标注“过程数据”，不得与论文最终数字并列而不解释。

处理统计差异时：

1. 不为了凑数删除、移动或修改无法确认的明细记录；
2. 先区分原始行数、清洗后行数、分析样本量和编码子样本；
3. 检查年度归属、账号改名、重复键、缺失值和边的关系语义；
4. 无法从现有证据确定的差异写入清洗报告，不编造排除原因；
5. 修改任一最终数字时，同步核对终版图、README、研究问题、自查结果和论文；论文成品未经用户明确授权不得修改。

## 逐行注释同步

`注释.py` 是唯一的逐行注释版，必须与 `x_scraper.py` 保持抽象语法树和有效代码标记等价。修改爬虫后应同步更新该文件。注释必须解释代码在当前语境中的真实作用，不得加入“空行分隔”等无意义说明，也不得改变 shebang、编码声明、多行字符串、JavaScript 模板或程序行为。

## 修改后验证

每次代码修改至少执行：

```bash
python3 -m py_compile x_scraper.py
python3 -m unittest -v
```

提交前必须确认：

- 所有测试通过；
- 没有新增Cookie、配置、缓存或采集输出到Git；
- `git diff` 中没有无关改动；
- README中的命令和实际CLI一致；
- 若CSV结构变化，已有分析流程已同步更新。

涉及页面选择器的修改，除自动测试外还需进行一次最小真实页面抽查，但不得在测试或日志中保存个人Cookie。

## Git提交范围

本项目的Git首先用于记录代码更迭，也可以在课程提交定稿时记录与代码直接相关的 README、harness、修复日志和依赖说明。每次仍应显式暂存文件，例如：

```bash
git add x_scraper.py test_x_scraper.py requirements.txt
```

禁止默认执行 `git add .`。原始数据、清洗数据、图表、真实配置和Cookie不进入Git。课程过程文档可以在一次明确命名的定稿提交中加入，以便教师从提交记录核对代码版本与说明材料。

任何时候都不得提交 `x_cookies*.json`、真实 `config.json`、`data/` 或包含个人凭据的日志。最终打包时必须保留 `.git/` 与 `data/`，但先将真实 Cookie 和配置移出提交文件夹。
